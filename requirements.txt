# Core dependencies for tact_module
# Adjust the PyTorch install if you require a specific CUDA build.
torch>=2.1.0
numpy>=1.23
transformers>=4.41
safetensors>=0.4

# Experiment tracking and logging
wandb>=0.15
pyyaml>=6.0
h5py>=3.9
